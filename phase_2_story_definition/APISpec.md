apistandards.md hiện chỉ là quy tắc viết API, chưa phải spec thực tế (endpoint list, request/response schema). AI cần spec thực để generate code đúng contract.

file này được sinh ra từ đâu và output mong muốn của nó sẽ trông như thế nào ? 